# Therefore Configuration Processor - Requirements

# Core dependencies (all standard library - no external packages required)
# The application uses only Python standard library modules:
# - tkinter (GUI)
# - xml.etree.ElementTree (XML parsing)
# - json, pathlib, datetime, etc.

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0

# Web interface dependencies (optional)
flask>=2.0.0

# Build/packaging dependencies (optional)
pyinstaller>=5.0.0
